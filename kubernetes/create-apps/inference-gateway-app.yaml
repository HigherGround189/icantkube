apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: api-gateway
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/HigherGround189/icantkube.git
    targetRevision: main
    path: kubernetes/apps/inference-gateway

  destination:
    server: https://kubernetes.default.svc
    namespace: inference-gateway

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
