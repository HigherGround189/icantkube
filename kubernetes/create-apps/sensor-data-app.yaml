apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sensor-data
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/HigherGround189/icantkube.git
    targetRevision: main
    path: kubernetes/apps/sensor-data
    # Eg: kubernetes/apps/frontend-test

  destination:
    server: https://kubernetes.default.svc
    namespace: msensor-data

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
