apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sensor-data
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/HigherGround189/icantkube.git
    targetRevision: main
    path: kubernetes/apps/model-pipeline/sensor-data
    # Eg: kubernetes/apps/frontend-test

  destination:
    server: https://kubernetes.default.svc
    namespace: model-pipeline

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
