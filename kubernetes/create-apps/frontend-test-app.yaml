apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-test
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend-test
  template:
    metadata:
      labels:
        app: frontend-test
    spec:
      containers:
        - name: frontend-test
          image: mysticshirou/icantkube:v0.1
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-test
  namespace: default
spec:
  selector:
    app: frontend-test
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-test
  namespace: default
spec:
  ingressClassName: cloudflare-tunnel
  rules:
    - host: frontend-test.argocd.help
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-test
                port:
                  number: 80
