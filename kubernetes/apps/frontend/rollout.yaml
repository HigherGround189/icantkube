apiVersion: argoproj.io/v1alpha1               
kind: Rollout
metadata:
  name: frontend-rollout
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  workloadRef: 
    apiVersion: apps/v1
    kind: Deployment
    name: frontend
    scaleDown: onsuccess
  strategy:
    canary:
      steps:
        - setWeight: 80
        - pause: {duration: 10s}
