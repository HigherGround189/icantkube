apiVersion: argoproj.io/v1alpha1               
kind: Rollout
metadata:
  name: sensor-data-rollout
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sensor-data
  workloadRef: 
    apiVersion: apps/v1
    kind: Deployment
    name: sensor-data-deployment
    scaleDown: onsuccess
  strategy:
    canary:
      steps:
        - setWeight: 80
        - pause: {duration: 10s}
