apiVersion: v1
kind: Service
metadata:
  name: mlflow
  namespace: model-pipeline
  labels:
    app: mlflow
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: mlflow
