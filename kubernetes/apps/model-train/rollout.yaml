apiVersion: argoproj.io/v1alpha1               
kind: Rollout
metadata:
  name: model-train-rollout
spec:
  replicas: 1
  selector:
    matchLabels:
      app: model-train
  workloadRef: 
    apiVersion: apps/v1
    kind: Deployment
    name: model-train
    scaleDown: onsuccess
  strategy:
    canary:
      steps:
        - setWeight: 80
        - pause: {duration: 10s}
