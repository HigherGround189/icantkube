apiVersion: argoproj.io/v1alpha1               
kind: Rollout
metadata:
  name: kubebox-rollout
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubebox
  workloadRef: 
    apiVersion: apps/v1
    kind: Deployment
    name: kubebox
    scaleDown: onsuccess
  strategy:
    canary:
      steps:
        - setWeight: 80
        - pause: {duration: 10s}
